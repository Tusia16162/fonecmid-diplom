
Процедура ОбработкаПроведения(Отказ, Режим)

	Движения.ВКМ_ГрафикОтпусков.Записывать = Истина;
	Для Каждого ТекСтрока Из ОтпускаСотрудников Цикл
		Движение = Движения.ВКМ_ГрафикОтпусков.Добавить();
		Движение.Год = Год;
		Движение.Сотрудник = ТекСтрока.Сотрудник;
		Движение.ДатаНачала = ТекСтрока.ДатаНачала;
		Движение.ДатаОкончания = ТекСтрока.ДатаОкончания; 
		Движение.КоличествоДнейЗаПериод = ТекСтрока.КоличествоДней; 
	КонецЦикла;

КонецПроцедуры
